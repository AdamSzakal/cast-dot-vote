import React from "react";
import "../../reset.css";
import "../../App.css";
import "../../utils.css";

function Grid({ children }) {
  return <section class="grid">{children}</section>;
}

export default Grid;
